# 06-02 Plan: Build And Manual Acceptance Notes

## Goal

Record the working build command and remaining manual acceptance dependency.

## Tasks

- Build Qizip with project-local DerivedData.
- Note that local `7zz` is missing, so successful command execution needs to be validated after installing SevenZip.

## Verification

- `xcodebuild ... -derivedDataPath .build/DerivedData CODE_SIGNING_ALLOWED=NO build`
