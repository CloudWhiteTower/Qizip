# 06-01 Plan: Parser And Planner Harness

## Goal

Cover the pure parsing and Smart Extract planning logic with runnable validation.

## Tasks

- Add a small Swift command-line harness under `Tests/`.
- Validate non-ASCII listing parsing.
- Validate single-top-level and multi-top-level Smart Extract destination behavior.

## Verification

- Compile and run the harness with `swiftc`.
