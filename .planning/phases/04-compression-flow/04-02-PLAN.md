# 04-02 Plan: Add Compression UI Integration

## Goal

Wire `Compress Files...` from the home view and dropped files into the compression flow.

## Tasks

- Enable home compression button.
- Add a lightweight `CompressSheet`.
- Append compression logs and show success/failure.

## Verification

- Build the Xcode target.
- Manual run: choose files/folders and create `.7z` and `.zip`.
