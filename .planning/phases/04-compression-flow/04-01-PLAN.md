# 04-01 Plan: Add Compression Service And Dialogs

## Goal

Add compression options, input selection, output selection, and service invocation.

## Tasks

- Add `CompressionOptions`.
- Add file/folder multi-selection dialog.
- Add save dialog for `.7z` and `.zip`.
- Add `ArchiveService.compress`.

## Verification

- Build the Xcode target.
- Manual run with paths containing spaces and non-ASCII characters.
