# 02-01 Plan: Add Archive Operation APIs

## Goal

Add service-layer operations for extract, test, info data, and reusable command result handling.

## Tasks

- Extend `ArchiveService` with extract and test methods.
- Add an archive info model that can be derived from current entries.
- Preserve missing-7zz and non-zero-exit error behavior.
- Keep all subprocess invocation based on executable URL plus argument arrays.

## Verification

- Build the Xcode target with code signing disabled.
- Confirm command arguments are arrays and paths are passed as URL-derived paths.
