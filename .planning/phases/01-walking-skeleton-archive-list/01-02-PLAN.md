---
phase: 01-walking-skeleton-archive-list
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Qizip/Views/EmptyHomeView.swift
  - Qizip/Views/ArchiveBrowserView.swift
  - Qizip/Views/LogView.swift
autonomous: true
requirements: [PLAT-01, ENG-01, ENG-02, BROW-01, BROW-02, LOG-01, UX-01, UX-02, UX-03]
user_setup: []
must_haves:
  truths:
    - EmptyHomeView displays the exact drop guidance copy.
    - EmptyHomeView shows Open Archive and Compress Files... controls.
    - EmptyHomeView displays current 7zz status.
    - ArchiveBrowserView displays entries in a SwiftUI Table with required columns.
    - LogView renders command output text.
  artifacts:
    - Qizip/Views/EmptyHomeView.swift
    - Qizip/Views/ArchiveBrowserView.swift
    - Qizip/Views/LogView.swift
  key_links:
    - ArchiveBrowserView consumes [ArchiveEntry].
    - EmptyHomeView exposes callbacks rather than owning archive service logic.
---

<objective>
Create the Phase 1 SwiftUI view layer: home screen, archive table browser, and log view.

Purpose: Make the service foundation visible through a clean macOS-native interface without embedding process or parsing logic in views.
Output: New SwiftUI view files under `Qizip/Views/`.
</objective>

<execution_context>
@/Users/cloud/code/CodeRepository/xcode_programmes/Qizip/Qizip/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cloud/code/CodeRepository/xcode_programmes/Qizip/Qizip/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton-archive-list/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-archive-list/01-UI-SPEC.md
@.planning/phases/01-walking-skeleton-archive-list/01-PATTERNS.md
@Qizip/Models/ArchiveEntry.swift
</context>

<threat_model>
Potential threats:
- UI accidentally triggers file access without user action.
- Large logs or table data make layout unusable.

Required mitigations:
- Views expose callbacks for user-initiated actions only.
- Keep log display scrollable.
- Use system colors and compact macOS controls.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build EmptyHomeView</name>
  <files>Qizip/Views/EmptyHomeView.swift</files>
  <read_first>.planning/phases/01-walking-skeleton-archive-list/01-UI-SPEC.md</read_first>
  <action>Create `EmptyHomeView` with inputs for sevenZip status text and callbacks for `Open Archive` and `Compress Files...`. Include exact text `Drop an archive to open, or drop files to compress.`. Keep compression action visually present but it may be disabled or call a deferred placeholder in Phase 1.</action>
  <verify>rg -n "Drop an archive to open, or drop files to compress.|Open Archive|Compress Files" Qizip/Views/EmptyHomeView.swift</verify>
  <acceptance_criteria>
    - Required home copy exists.
    - Required controls exist.
    - View does not import or call `Process`.
  </acceptance_criteria>
  <done>Home view can be rendered from ContentView with callbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Build ArchiveBrowserView</name>
  <files>Qizip/Views/ArchiveBrowserView.swift</files>
  <read_first>.planning/REQUIREMENTS.md, Qizip/Models/ArchiveEntry.swift</read_first>
  <action>Create `ArchiveBrowserView` that accepts archive URL/name, `[ArchiveEntry]`, loading/error state as needed, and log text. Use SwiftUI `Table` with columns Name, Path, Size, Modified, and Is Directory. Do not add Extract/Test/Info toolbar actions in this phase.</action>
  <verify>rg -n "Table|Name|Path|Size|Modified|Is Directory" Qizip/Views/ArchiveBrowserView.swift</verify>
  <acceptance_criteria>
    - SwiftUI Table is used.
    - All required columns are visible.
    - View consumes `ArchiveEntry`.
  </acceptance_criteria>
  <done>Archive contents can be rendered once ContentView supplies entries.</done>
</task>

<task type="auto">
  <name>Task 3: Build LogView</name>
  <files>Qizip/Views/LogView.swift</files>
  <read_first>.planning/phases/01-walking-skeleton-archive-list/01-UI-SPEC.md</read_first>
  <action>Create `LogView` for scrollable display of stdout/stderr text. Use monospaced text, system colors, and a stable max/min height suitable for embedding below the table.</action>
  <verify>rg -n "ScrollView|monospaced|LogView" Qizip/Views/LogView.swift</verify>
  <acceptance_criteria>
    - Log output is scrollable.
    - Log view is reusable and does not own process execution.
  </acceptance_criteria>
  <done>Command output has a visible UI target.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Drop an archive to open, or drop files to compress.|Table|LogView" Qizip/Views`
- [ ] `! rg -n "Process\\(" Qizip/Views`
- [ ] `xcodebuild -project Qizip.xcodeproj -scheme Qizip -destination 'platform=macOS' build`
</verification>

<success_criteria>

- Required Phase 1 views exist.
- Views are callback-driven and do not own subprocess logic.
- Archive entries can render in a Table.
- Build succeeds.

</success_criteria>

<output>
After completion, create `.planning/phases/01-walking-skeleton-archive-list/01-02-SUMMARY.md`
</output>

