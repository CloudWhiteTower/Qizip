---
phase: 01-walking-skeleton-archive-list
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - Qizip/ContentView.swift
autonomous: true
requirements: [PLAT-01, PLAT-02, OPEN-01, OPEN-02, OPEN-03, OPEN-04, OPEN-05, BROW-01, BROW-02, LOG-01, LOG-02, UX-01, UX-02, UX-03, PATH-01]
user_setup:
  - Install 7zz with `brew install sevenzip` for full manual archive listing verification if it is not already installed.
must_haves:
  truths:
    - ContentView routes between EmptyHomeView and ArchiveBrowserView.
    - Open Archive uses FileDialogs.openArchive and ArchiveService.listArchive.
    - Dragged archive URLs route through the same listing flow as the open panel.
    - UI remains responsive while listing is running.
    - Missing 7zz is surfaced without app crash.
  artifacts:
    - Qizip/ContentView.swift
  key_links:
    - ContentView connects FileDialogs, ArchiveService, EmptyHomeView, ArchiveBrowserView, and LogView.
    - Drag/drop flow and open-panel flow share one archive-opening method.
---

<objective>
Wire the Phase 1 walking skeleton together in `ContentView`: service state, open panel, drag/drop, archive listing, table routing, log updates, and manual verification.

Purpose: Complete the smallest runnable Qizip app slice without implementing later archive actions.
Output: Updated root SwiftUI content that launches into the Qizip archive-open/list experience.
</objective>

<execution_context>
@/Users/cloud/code/CodeRepository/xcode_programmes/Qizip/Qizip/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cloud/code/CodeRepository/xcode_programmes/Qizip/Qizip/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton-archive-list/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-archive-list/01-UI-SPEC.md
@.planning/phases/01-walking-skeleton-archive-list/01-VALIDATION.md
@Qizip/ContentView.swift
@Qizip/Services/ArchiveService.swift
@Qizip/Utilities/FileDialogs.swift
@Qizip/Views/EmptyHomeView.swift
@Qizip/Views/ArchiveBrowserView.swift
</context>

<threat_model>
Potential threats:
- Drag/drop accepts unsupported files and sends them to `7zz`.
- Async work updates UI state off the main actor.
- Logs or error messages are lost, making failures opaque.

Required mitigations:
- Filter drop URLs by supported archive extension before listing.
- Keep state updates on the main actor.
- Append stdout, stderr, and user-facing errors to the log.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder ContentView with app state orchestration</name>
  <files>Qizip/ContentView.swift</files>
  <read_first>Qizip/ContentView.swift, Qizip/Services/ArchiveService.swift, Qizip/Views/EmptyHomeView.swift, Qizip/Views/ArchiveBrowserView.swift</read_first>
  <action>Replace the placeholder globe/Hello World UI. Add state for selected archive URL, entries, log text, loading flag, error message, and `SevenZipLocator` status. Render `EmptyHomeView` when no archive is loaded and `ArchiveBrowserView` when an archive URL exists.</action>
  <verify>rg -n "EmptyHomeView|ArchiveBrowserView|ArchiveService|selectedArchive" Qizip/ContentView.swift</verify>
  <acceptance_criteria>
    - Placeholder `Hello, world!` UI is removed.
    - ContentView references the new views and service.
  </acceptance_criteria>
  <done>Root view can switch between home and archive browser states.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Open Archive flow</name>
  <files>Qizip/ContentView.swift</files>
  <read_first>Qizip/Utilities/FileDialogs.swift, Qizip/Services/ArchiveService.swift</read_first>
  <action>Add an `openArchive()` method that calls `FileDialogs.openArchive()`, then `loadArchive(url:)`. In `loadArchive(url:)`, set loading state, call `ArchiveService.listArchive(url:)` from an async `Task`, update entries and log text, and handle missing 7zz/process/parser errors without crashing.</action>
  <verify>rg -n "openArchive|loadArchive|Task|listArchive" Qizip/ContentView.swift</verify>
  <acceptance_criteria>
    - Open panel and service listing are connected.
    - Async listing does not block the main UI.
    - Errors are visible in state/log.
  </acceptance_criteria>
  <done>User can click Open Archive and get a listing or clear error.</done>
</task>

<task type="auto">
  <name>Task 3: Wire drag and drop archive flow</name>
  <files>Qizip/ContentView.swift</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>Add SwiftUI drop handling for file URLs. Accept supported extensions `7z`, `zip`, `rar`, `tar`, `gz`, `bz2`, `xz`, `zst`, `iso`, `dmg`, `cab`, `wim`. Route the first supported archive URL through the same `loadArchive(url:)` method as the open panel.</action>
  <verify>rg -n "onDrop|7z|zip|rar|wim|loadArchive" Qizip/ContentView.swift</verify>
  <acceptance_criteria>
    - Drop flow supports all required extensions.
    - Drop flow and open-panel flow share listing logic.
  </acceptance_criteria>
  <done>User can drop a supported archive to list it.</done>
</task>

<task type="auto">
  <name>Task 4: Run Phase 1 verification</name>
  <files>Qizip/ContentView.swift</files>
  <read_first>.planning/phases/01-walking-skeleton-archive-list/01-VALIDATION.md</read_first>
  <action>Run static checks for subprocess safety and build the app. If `xcodebuild` cannot run in the environment, record the exact blocker in the summary and still provide manual Xcode verification steps.</action>
  <verify>xcodebuild -project Qizip.xcodeproj -scheme Qizip -destination 'platform=macOS' build</verify>
  <acceptance_criteria>
    - Build command is attempted.
    - Static checks confirm no shell-string process invocation in services.
    - Summary documents manual verification results or blockers.
  </acceptance_criteria>
  <done>Phase 1 can be verified through build/static/manual checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "Process\\.executableURL|Process\\.arguments|executableURL|arguments" Qizip/Services`
- [ ] `! rg -n "/bin/sh|bash|zsh|-c" Qizip/Services`
- [ ] `rg -n "Drop an archive to open, or drop files to compress.|Open Archive|Table|LogView" Qizip`
- [ ] `xcodebuild -project Qizip.xcodeproj -scheme Qizip -destination 'platform=macOS' build`
</verification>

<success_criteria>

- User can launch Qizip and see home view with required copy, controls, and 7zz status.
- User can open or drop a supported archive.
- `7zz l -slt` listing uses argument arrays and populates the table.
- stdout/stderr appears in LogView.
- Missing `7zz` shows exact guidance without crash.

</success_criteria>

<output>
After completion, create `.planning/phases/01-walking-skeleton-archive-list/01-03-SUMMARY.md`
</output>

